/// The theme-selection mode for [LayrzApp].
enum LayrzThemeMode {
  /// Always use the light theme.
  light,

  /// Always use the dark theme.
  dark,

  /// Follow the operating system's brightness setting.
  system,
}
