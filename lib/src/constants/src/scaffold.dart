import 'package:flutter/widgets.dart';

// Structural constants for [LayrzScaffoldShell] layout and styling.
// These constants define exact pixel values, spacing, and typography
// specifications for the scaffold list panel, detail pane, and interactive elements.

/// List panel width in logical pixels.
const double kLayrzScaffoldListWidth = 250.0;

/// List panel header padding: left and right in logical pixels.
const double kLayrzScaffoldHeaderHorizontalPadding = 12.0;

/// List panel header padding: top in logical pixels.
const double kLayrzScaffoldHeaderTopPadding = 12.0;

/// List panel header padding: bottom in logical pixels.
const double kLayrzScaffoldHeaderBottomPadding = 10.0;

/// Gap between list header title and count badge in logical pixels.
const double kLayrzScaffoldHeaderGap = 9.0;

/// List panel header title font size in logical pixels.
const double kLayrzScaffoldHeaderTitleFontSize = 13.5;

/// List panel header count badge font size in logical pixels.
const double kLayrzScaffoldHeaderCountFontSize = 11.0;

/// List panel filter field height in logical pixels.
const double kLayrzScaffoldFilterHeight = 30.0;

/// List panel filter field horizontal padding in logical pixels.
const double kLayrzScaffoldFilterHorizontalPadding = 10.0;

/// List panel filter field font size in logical pixels.
const double kLayrzScaffoldFilterFontSize = 13.0;

/// List panel filter field leading icon size in logical pixels.
const double kLayrzScaffoldFilterIconSize = 12.0;

/// List panel body padding in logical pixels.
const double kLayrzScaffoldBodyPadding = 6.0;

/// Gap between list items in the body in logical pixels.
const double kLayrzScaffoldBodyGap = 1.0;

/// List item vertical padding in logical pixels.
const double kLayrzScaffoldListItemVerticalPadding = 9.0;

/// List item horizontal padding in logical pixels.
const double kLayrzScaffoldListItemHorizontalPadding = 8.0;

/// List item border radius in logical pixels.
const double kLayrzScaffoldListItemRadius = 9.0;

/// Gap between list item icon and label in logical pixels.
const double kLayrzScaffoldListItemGap = 10.0;

/// List item icon tile size (width and height) in logical pixels.
const double kLayrzScaffoldListItemIconTileSize = 20.0;

/// List item icon tile border radius in logical pixels.
const double kLayrzScaffoldListItemIconTileRadius = 6.0;

/// List item icon size in logical pixels.
const double kLayrzScaffoldListItemIconSize = 11.0;

/// List item label font size in logical pixels.
const double kLayrzScaffoldListItemLabelFontSize = 12.5;

/// List item meta (subtitle) font size in logical pixels.
const double kLayrzScaffoldListItemMetaFontSize = 10.5;

/// Font weight of a selected list item's label.
const FontWeight kLayrzScaffoldListItemSelectedLabelFontWeight = FontWeight.w600;

/// Font weight of an unselected list item's label.
const FontWeight kLayrzScaffoldListItemUnselectedLabelFontWeight = FontWeight.w400;

/// Group header sticky background padding: vertical in logical pixels.
const double kLayrzScaffoldGroupHeaderVerticalPadding = 7.0;

/// Group header sticky background padding: horizontal in logical pixels.
const double kLayrzScaffoldGroupHeaderHorizontalPadding = 13.0;

/// Group header font size in logical pixels.
const double kLayrzScaffoldGroupHeaderFontSize = 10.0;

/// Font weight of the group header.
const FontWeight kLayrzScaffoldGroupHeaderFontWeight = FontWeight.w600;

/// Group header letter spacing as a multiple of font size.
const double kLayrzScaffoldGroupHeaderLetterSpacing = 0.09;

/// Empty state padding: vertical in logical pixels.
const double kLayrzScaffoldEmptyStateVerticalPadding = 18.0;

/// Empty state padding: horizontal in logical pixels.
const double kLayrzScaffoldEmptyStateHorizontalPadding = 10.0;

/// Empty state text font size in logical pixels.
const double kLayrzScaffoldEmptyStateFontSize = 12.0;

/// List panel footer padding: vertical in logical pixels.
const double kLayrzScaffoldFooterVerticalPadding = 10.0;

/// List panel footer padding: horizontal in logical pixels.
const double kLayrzScaffoldFooterHorizontalPadding = 12.0;

/// List panel footer font size in logical pixels.
const double kLayrzScaffoldFooterFontSize = 10.5;

/// Detail pane header padding: vertical in logical pixels.
const double kLayrzScaffoldDetailHeaderVerticalPadding = 14.0;

/// Detail pane header padding: horizontal in logical pixels.
const double kLayrzScaffoldDetailHeaderHorizontalPadding = 26.0;

/// Detail pane header gap between title/subtitle and actions in logical pixels.
const double kLayrzScaffoldDetailHeaderGap = 18.0;

/// Detail pane header title font size in logical pixels.
const double kLayrzScaffoldDetailHeaderTitleFontSize = 17.0;

/// Font weight of the detail pane header title.
const FontWeight kLayrzScaffoldDetailHeaderTitleFontWeight = FontWeight.w700;

/// Detail pane header subtitle font size in logical pixels.
const double kLayrzScaffoldDetailHeaderSubtitleFontSize = 11.5;

/// Detail pane body padding in logical pixels.
const double kLayrzScaffoldDetailBodyPadding = 26.0;

/// Detail pane body gap between sections in logical pixels.
const double kLayrzScaffoldDetailBodyGap = 20.0;

/// Detail pane maximum content width in logical pixels.
const double kLayrzScaffoldDetailMaxWidth = 1080.0;

/// List item selected row background opacity (7%).
const double kLayrzScaffoldListItemSelectedRowBackgroundOpacity = 0.07;

/// List item selected icon tile background opacity (12%).
const double kLayrzScaffoldListItemSelectedIconTileBackgroundOpacity = 0.12;

/// List item hover background opacity (4%).
const double kLayrzScaffoldListItemHoverBackgroundOpacity = 0.04;

/// Grouped/flat toggle button container height in logical pixels.
const double kLayrzScaffoldToggleHeight = 28.0;

/// Grouped/flat toggle button container border radius in logical pixels.
const double kLayrzScaffoldToggleBorderRadius = 8.0;

/// Grouped/flat toggle button segment padding: horizontal in logical pixels.
const double kLayrzScaffoldToggleSegmentHorizontalPadding = 10.0;

/// Grouped/flat toggle button segment padding: vertical in logical pixels.
const double kLayrzScaffoldToggleSegmentVerticalPadding = 4.0;

/// Grouped/flat toggle button font size in logical pixels.
const double kLayrzScaffoldToggleFontSize = 11.0;

/// Font weight of the grouped/flat toggle button.
const FontWeight kLayrzScaffoldToggleFontWeight = FontWeight.w500;
