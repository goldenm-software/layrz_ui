/// Icon glyph size inside a [LayrzChip].
///
/// This is the size of the icon asset (in logical pixels) when rendered in
/// a [LayrzChip], whether leading or delete affordance.
const double kLayrzChipIconSize = 18.0;
